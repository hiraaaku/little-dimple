export const metadata = {
    title: 'Dimple Squad - Little Dimple`',
    description: 'Dimple Squad',
}

export default function DimpleSquadLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    )
}
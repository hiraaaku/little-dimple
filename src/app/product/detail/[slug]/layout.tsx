export const metadata = {
    title: 'Product Detail - Little Dimple`',
    description: 'Product Detail',
}

export default function ProductDetailLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    )
}